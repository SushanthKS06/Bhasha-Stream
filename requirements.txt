# Core Web Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Async HTTP Client (used by LLM service for vLLM SSE streaming)
aiohttp==3.9.5

# Audio & ML Inference
numpy==1.26.4
scipy==1.13.0
onnxruntime-gpu==1.18.0
faster-whisper==1.0.3
ctranslate2==4.3.1

# LLM Client (openai SDK as fallback; aiohttp used directly for SSE streaming)
httpx==0.27.0
openai==1.30.1

# TTS
# MeloTTS must be installed from source — PyPI package is unofficial/outdated:
#   pip install git+https://github.com/myshell-ai/MeloTTS.git
# For Docker builds, use the RUN directive in Dockerfile.backend directly.
# melo-tts is NOT a valid PyPI package name; do not use pip install melo-tts.

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# Utilities
aiofiles==24.1.0
loguru==0.7.2